# Placeholder for solid_quad.png

This reference image will be automatically generated when you run:

```bash
UPDATE_VISUAL_REFERENCES=1 cargo test visual_regression
```

The image shows a solid colored quad with:
- Color: RGB(0.2, 0.3, 0.8) - Blue
- Size: 256x256 pixels
- Format: RGBA PNG

The quad is rendered using GPU and captured for visual regression testing.
