# Placeholder for triangle.png

This reference image will be automatically generated when you run:

```bash
UPDATE_VISUAL_REFERENCES=1 cargo test visual_regression
```

The image shows a colored triangle with:
- Red vertex at top
- Green vertex at bottom-left
- Blue vertex at bottom-right
- Size: 256x256 pixels
- Format: RGBA PNG

The triangle is rendered using GPU and captured for visual regression testing.
